 {% extends 'base.html.twig' %}


{% block title %}Création d'une sortie{% endblock %}

{% block body %}
    <h1>Création d'une sortie</h1>

    {{ form_start(sortieForm) }}
    {{ form_widget(sortieForm) }}
    <a href="{{ path('lieu_creation') }}">Ajouter un autre lieu</a>
    <br>

    <button type="submit" class="btn">Enregistrer</button>
    <button type="submit" class="btn">Publier la sortie</button>
    <a href="{{ path('sortie_liste') }}" class="btn btn-secondary">Annuler</a>
    {{ form_end(sortieForm) }}


{% endblock %}



